import { Beer } from "types/shared.types";

export type BeersPageProps = {
  staticBeers: Beer[]
}
export type FeaturedBeerProps = {
  id: string;
}
export type StaticBeersListProps = {
  beers: Beer[]
}
